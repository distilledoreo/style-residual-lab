# Local Target Corpus

This directory is local-only and is not committed to the repository.

Put one folder per work here, each containing a `work.txt` file:

```text
target-corpus/
  Work Title/
    work.txt
```

Run `npm run import` to copy these into `data/raw/target/lyrics/` before running the pipeline.
